<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rialto Web" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p>
      <%= link_to 'RIALTO Home', root_path %> |
      <% if current_user.present? %>
        Logged in: <%= current_user.name %> | Groups: <%= Current.groups.join(', ') %> | <%= link_to 'Logout', logout_path %>
      <% else %>
        <%= link_to 'Login', login_path(referrer: request.original_url) %>
      <% end %>
      <ul>
        <li><%= link_to 'public', controller: :public, action: :index %></li>
        <% if current_user.present? && allowed_to?(:view?, :stanford) %>
          <li><%= link_to 'stanford', controller: :stanford, action: :index %></li>
        <% end %>
        <% if current_user.present? && allowed_to?(:view?, :restricted) %>
          <li><%= link_to 'restricted', controller: :restricted, action: :index %></li>
        <% end %>
      </ul>
    </p>

    <% flash.each do |type, message| %>
      <div class="flash <%= type %>"><%= message %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
